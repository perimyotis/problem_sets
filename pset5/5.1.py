import pysam 

open ("Intersect_filtered_cov8_chr21_rand1000.bed") as f:
	for line in f:
		L =line.strip().split()

		for given site:
		get allele freq

			if freq == 0:
				not seq
			elif freq == n:
				not seq
			else: 
				seg =+1
	
	
